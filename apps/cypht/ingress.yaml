---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cypht-ingress
  annotations:
    traefik.ingress.k8s.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: cloudflare-issuer
spec:
  rules:
    - host: mail.${DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cypht-service
                port:
                  number: 80
  tls:
    - hosts:
        - mail.${DOMAIN}
      secretName: cypht-tls
